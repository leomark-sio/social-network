# Social Network API

Express.js REST API for the Social Network application.

## Setup

1. Install dependencies: `yarn install`
2. Copy `.env.example` to `.env` and configure
3. Run: `yarn dev`

See the [root README](../README.md) for full documentation.
